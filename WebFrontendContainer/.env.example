# Backend API base URL (no auth expected in this preview)
REACT_APP_API_BASE_URL=http://localhost:8000/api/v1

# Optional API debug logging
REACT_APP_API_DEBUG=false

# Feature flag to guard auth code paths (auth disabled by default)
AUTH_ENABLED=false
